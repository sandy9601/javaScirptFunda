// ! funcions are reusable code that executes a task

// * we can declare functions by using function key word

// ? example
let ouerName="sana" // ! outer varaible

function showMessage(message){
    let displayName = "sandy" // ! local variables
    console.log(message,displayName)
}

showMessage("hi")
showMessage("hello")
showMessage("namaste")

// ! here we created the function showMessage and we can use it to display any message we want


