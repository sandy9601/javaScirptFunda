// the approach of non-blocking I/O. In Non-blocking I/O approach,
//  you can initiate a request in parallel for user2 without waiting for the response
//   to the request for user1. The requests in Node.js can initiate in parallel.
// This non-blocking I/O eliminates the need for multi-threading.


console.log(Object.getOwnPropertyNames(require));
console.log(require)